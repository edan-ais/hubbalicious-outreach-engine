services:
  - type: cron
    name: hubbalicious-outreach
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python multi_sender_outreach.py"
    schedule: "0 */2 * * *"   # every 2 hours
